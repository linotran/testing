Binôme : Clarisse LOU, Lino TRAN

Q.1b : Oui, il faut créer des tests à part pour getX et getY.

Q.2b : Non, car les tests ne prennent pas en compte toutes les valeurs possibles en entrée.

Q.3a : La méthode setPoint fait appel à une autre classe (Random) qui n'est pas testée. Nous devons donc utiliser des mocks afin d'avoir des valeurs viables en entrée.